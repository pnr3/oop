#pragma once
#include "Car.h"
class Engineer {
public:
	int getCarPrice(Car*);
	int getCarWheels(Car*);
	int getCarwheels(Car*);
	float getCarSpeed(Car*);
	void getCarName(Car*);
	float getCarspeed(Car*);
};